# Infrastructure Scripts

Future helper scripts can wrap validation and deployment commands, such as:

- Bicep build.
- Azure What-If.
- Policy checks.
- Environment bootstrap checks.

Scripts must not embed credentials or subscription-specific secrets.

