# Azure SQL Operations Module

This module captures implementation-ready intent for Azure SQL Managed Instance operations. It is not deployed by default. Environment parameter files keep `deployAzureSqlOperations` disabled until a real Azure validation context exists.

